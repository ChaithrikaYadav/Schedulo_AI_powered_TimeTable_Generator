# ── Schedulo Local Prototype Requirements ──────────────────────────────────────
# Install: pip install -r requirements.local.txt
# Python 3.11+ required (tested on 3.11, 3.12, 3.14)
# All versions pinned to working combinations as of March 2026

# ── Core Web Framework ──────────────────────────────────────────────────────────
fastapi>=0.111.0,<0.116.0
uvicorn[standard]>=0.29.0,<0.35.0
python-multipart>=0.0.9
websockets>=12.0,<14.0

# ── Database ────────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.30,<2.1.0
alembic>=1.13.1,<1.15.0
aiosqlite>=0.20.0              # SQLite async driver (default for local dev)
asyncpg>=0.29.0                # PostgreSQL driver (optional, for Docker mode)

# ── Auth ────────────────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
fastapi-users[sqlalchemy]>=13.0.0

# ── Config ──────────────────────────────────────────────────────────────────────
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
python-dotenv>=1.0.1

# ── Data / CSV ──────────────────────────────────────────────────────────────────
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<2.0.0
openpyxl>=3.1.2

# ── Scheduling Algorithms ────────────────────────────────────────────────────────
ortools>=9.10.4067             # Google OR-Tools CSP/CP-SAT
deap>=1.4.1                    # Genetic algorithms
scikit-learn>=1.4.0,<2.0.0    # ML pipeline + Isolation Forest

# ── Machine Learning (lightweight — CPU only, no GPU required) ────────────────────
xgboost>=2.0.3,<3.0.0         # Quality predictor (~15MB)
lightgbm>=4.3.0,<5.0.0        # Alternative quality predictor (~5MB)
optuna>=3.6.1,<5.0.0          # Hyperparameter tuning
# NOTE: difflib is Python standard library — DO NOT add difflib2 (it does not exist on PyPI)

# ── PyTorch (CPU-only build — works on any machine without CUDA) ─────────────────
# IMPORTANT: Do NOT change torch>=2.2.0 without the extra index URL below.
# Without it, pip may download a 3GB+ CUDA build that fails on machines without GPU.
torch>=2.2.0,<2.4.0
--extra-index-url https://download.pytorch.org/whl/cpu
# RL packages — needed only for the RL training phase (optional)
stable-baselines3>=2.3.2
gymnasium>=0.29.1

# ── AI Agents / LLM ──────────────────────────────────────────────────────────────
langgraph>=0.1.0,<0.3.0
langchain>=0.2.0,<0.4.0
langchain-community>=0.2.0,<0.4.0
huggingface_hub>=0.23.0,<0.25.0
transformers>=4.41.0,<4.46.0
groq>=0.9.0                    # Groq API client for fast LLM inference

# ── Export ───────────────────────────────────────────────────────────────────────
reportlab>=4.2.0               # PDF generation
python-docx>=1.1.0             # Word document generation

# ── Task Queue ───────────────────────────────────────────────────────────────────
celery>=5.4.0,<5.6.0
redis>=5.0.4,<6.0.0            # Redis client (for Celery broker)

# ── Testing ───────────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
factory-boy>=3.3.0
hypothesis>=6.103.0
httpx>=0.27.0                  # async test client for FastAPI

# ── Dev Tools ─────────────────────────────────────────────────────────────────────
structlog>=24.1.0
faker>=24.0.0
